[
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "",
    "section": "",
    "text": "ProjectOverview Code\n\n\n\n\n#| include: false\nlibrary(dplyr) library(readr)\nf_valid &lt;- file.path( “~/Symobio/Data/Processed/BF_NEE_Diagnostics”, “bfnee_valid_countries.csv” )\nf_diag &lt;- file.path( “~/Symobio/Data/Processed/BF_NEE_Diagnostics”, “bfnee_country_diagnostics_all_transitions.csv” )\nvalid &lt;- read_csv( f_valid, show_col_types = FALSE )\ndiag &lt;- read_csv( f_diag, show_col_types = FALSE )\nn_valid &lt;- valid %&gt;% filter(valid_for_analysis) %&gt;% summarise(n = n_distinct(iso3)) %&gt;% pull(n)\nn_processed &lt;- valid %&gt;% summarise(n = n_distinct(iso3)) %&gt;% pull(n)\nn_transitions &lt;- diag %&gt;% summarise(n = n_distinct(transition)) %&gt;% pull(n)\nn_outputs &lt;- nrow(diag)",
    "crumbs": [
      "Overview",
      "Project",
      "Overview"
    ]
  },
  {
    "objectID": "00_methodological_framework.html",
    "href": "00_methodological_framework.html",
    "title": "Methodological framework",
    "section": "",
    "text": "Show code\nlibrary(dplyr)\nlibrary(tibble)\nlibrary(knitr)",
    "crumbs": [
      "Overview",
      "Project",
      "Methodological framework"
    ]
  },
  {
    "objectID": "00_methodological_framework.html#step-1-remove-excluded-classes",
    "href": "00_methodological_framework.html#step-1-remove-excluded-classes",
    "title": "Methodological framework",
    "section": "Step 1: Remove excluded classes",
    "text": "Step 1: Remove excluded classes\nThe excluded classes are set to zero.\nThe remaining ecosystem shares are renormalized so that the non-excluded classes sum to one.",
    "crumbs": [
      "Overview",
      "Project",
      "Methodological framework"
    ]
  },
  {
    "objectID": "00_methodological_framework.html#step-2-match-agriculture-to-landshift",
    "href": "00_methodological_framework.html#step-2-match-agriculture-to-landshift",
    "title": "Methodological framework",
    "section": "Step 2: Match agriculture to LandSHIFT",
    "text": "Step 2: Match agriculture to LandSHIFT\nThe total GlobES agricultural share is calculated as the sum of classes 14.1 and 15.8.\nThis agricultural share is replaced by the LandSHIFT agricultural share.",
    "crumbs": [
      "Overview",
      "Project",
      "Methodological framework"
    ]
  },
  {
    "objectID": "00_methodological_framework.html#step-3-rescale-remaining-ecosystems",
    "href": "00_methodological_framework.html#step-3-rescale-remaining-ecosystems",
    "title": "Methodological framework",
    "section": "Step 3: Rescale remaining ecosystems",
    "text": "Step 3: Rescale remaining ecosystems\nAll non-agricultural ecosystem classes are rescaled so that the full ecosystem composition again sums to one.",
    "crumbs": [
      "Overview",
      "Project",
      "Methodological framework"
    ]
  },
  {
    "objectID": "03_country_drivers.html",
    "href": "03_country_drivers.html",
    "title": "Country drivers",
    "section": "",
    "text": "Show code\nlibrary(dplyr)\nlibrary(readr)\nlibrary(tidyr)\nlibrary(ggplot2)\nlibrary(scales)\nlibrary(DT)\n\ndir_diag &lt;- \"~/Symobio/Data/Processed/BF_NEE_Diagnostics\"\n\ndiag &lt;- read_csv(\n  file.path(dir_diag, \"bfnee_country_diagnostics_all_transitions.csv\"),\n  show_col_types = FALSE\n)\n\nvalid &lt;- read_csv(\n  file.path(dir_diag, \"bfnee_valid_countries.csv\"),\n  show_col_types = FALSE\n)\n\nvalid_iso3 &lt;- valid %&gt;%\n  filter(valid_for_analysis) %&gt;%\n  pull(iso3)\n\n\n\nCountry contributions to global BF-NEE\n\n\nShow code\ncountry_trend &lt;- diag %&gt;%\n  filter(\n    iso3 %in% valid_iso3,\n    status == \"ok\",\n    suspicious_old_scale == FALSE\n  ) %&gt;%\n  group_by(iso3, year, transition) %&gt;%\n  summarise(\n    bfnee_km2 = sum(bfnee_sum_km2, na.rm = TRUE),\n    natural_loss_km2 = sum(natural_loss_sum_km2, na.rm = TRUE),\n    weight_sum = mean(weight_sum, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\nglobal_by_year &lt;- country_trend %&gt;%\n  group_by(year, transition) %&gt;%\n  summarise(\n    global_bfnee_km2 = sum(bfnee_km2, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\ncountry_trend &lt;- country_trend %&gt;%\n  left_join(global_by_year, by = c(\"year\", \"transition\")) %&gt;%\n  mutate(\n    share_global_bfnee = bfnee_km2 / global_bfnee_km2\n  )\n\n\n\n\nTop countries by period\n\n\nShow code\ntop_countries_by_period &lt;- country_trend %&gt;%\n  group_by(year, transition) %&gt;%\n  slice_max(bfnee_km2, n = 15, with_ties = FALSE) %&gt;%\n  ungroup()\n\nggplot(\n  top_countries_by_period,\n  aes(\n    x = reorder(iso3, bfnee_km2),\n    y = bfnee_km2\n  )\n) +\n  geom_col() +\n  coord_flip() +\n  facet_wrap(~ transition, scales = \"free_y\") +\n  scale_y_continuous(labels = comma) +\n  labs(\n    title = \"Top BF-NEE countries by transition\",\n    subtitle = \"Validated countries only\",\n    x = \"Country\",\n    y = \"BF-NEE (km²)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nTop countries in latest period\n\n\nShow code\nlatest_year &lt;- max(country_trend$year, na.rm = TRUE)\n\ntop_latest &lt;- country_trend %&gt;%\n  filter(year == latest_year) %&gt;%\n  arrange(desc(bfnee_km2)) %&gt;%\n  slice_head(n = 20)\n\nggplot(\n  top_latest,\n  aes(\n    x = reorder(iso3, bfnee_km2),\n    y = bfnee_km2\n  )\n) +\n  geom_col() +\n  coord_flip() +\n  scale_y_continuous(labels = comma) +\n  labs(\n    title = paste(\"Top BF-NEE countries in\", latest_year),\n    subtitle = \"Validated countries only\",\n    x = \"Country\",\n    y = \"BF-NEE (km²)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nShare of global BF-NEE\n\n\nShow code\nggplot(\n  top_latest,\n  aes(\n    x = reorder(iso3, share_global_bfnee),\n    y = share_global_bfnee\n  )\n) +\n  geom_col() +\n  coord_flip() +\n  scale_y_continuous(labels = percent_format(accuracy = 1)) +\n  labs(\n    title = paste(\"Share of global BF-NEE in\", latest_year),\n    subtitle = \"Top 20 countries, validated countries only\",\n    x = \"Country\",\n    y = \"Share of global BF-NEE\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nCountry trajectories\n\n\nShow code\ntop_overall &lt;- country_trend %&gt;%\n  group_by(iso3) %&gt;%\n  summarise(\n    total_bfnee_km2 = sum(bfnee_km2, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  slice_max(total_bfnee_km2, n = 10, with_ties = FALSE) %&gt;%\n  pull(iso3)\n\nggplot(\n  country_trend %&gt;% filter(iso3 %in% top_overall),\n  aes(\n    x = year,\n    y = bfnee_km2,\n    group = iso3,\n    linetype = iso3\n  )\n) +\n  geom_line() +\n  geom_point() +\n  scale_y_continuous(labels = comma) +\n  labs(\n    title = \"BF-NEE trajectories for top countries\",\n    subtitle = \"Top 10 countries by total BF-NEE across all periods\",\n    x = \"Year\",\n    y = \"BF-NEE (km²)\",\n    linetype = \"Country\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nDiagnostic table\n\n\nShow code\ndatatable(\n  country_trend %&gt;%\n    arrange(desc(bfnee_km2)),\n  options = list(pageLength = 25)\n)",
    "crumbs": [
      "Overview",
      "Results",
      "Country drivers"
    ]
  },
  {
    "objectID": "02_global_trends.html",
    "href": "02_global_trends.html",
    "title": "Global BF-NEE trends",
    "section": "",
    "text": "Show code\nlibrary(dplyr)\nlibrary(readr)\nlibrary(tidyr)\nlibrary(ggplot2)\nlibrary(scales)\n\ndir_diag &lt;- \"~/Symobio/Data/Processed/BF_NEE_Diagnostics\"\n\ndiag &lt;- read_csv(\n  file.path(\n    dir_diag,\n    \"bfnee_country_diagnostics_all_transitions.csv\"\n  ),\n  show_col_types = FALSE\n)\n\nvalid &lt;- read_csv(\n  file.path(\n    dir_diag,\n    \"bfnee_valid_countries.csv\"\n  ),\n  show_col_types = FALSE\n)\n\nvalid_iso3 &lt;- valid %&gt;%\n  filter(valid_for_analysis) %&gt;%\n  pull(iso3)\n\n\n\nOverview\nThis page summarizes global BF-NEE trends based on countries that passed all processing and scale diagnostics.\n\n\nShow code\nglobal_trend &lt;- diag %&gt;%\n  filter(\n    iso3 %in% valid_iso3,\n    status == \"ok\",\n    suspicious_old_scale == FALSE\n  ) %&gt;%\n  group_by(year, transition) %&gt;%\n  summarise(\n    global_bfnee_km2 =\n      sum(bfnee_sum_km2, na.rm = TRUE),\n\n    global_natural_loss_km2 =\n      sum(natural_loss_sum_km2, na.rm = TRUE),\n\n    n_countries =\n      n_distinct(iso3),\n\n    .groups = \"drop\"\n  ) %&gt;%\n  arrange(year) %&gt;%\n  mutate(\n    bfnee_index =\n      global_bfnee_km2 /\n      first(global_bfnee_km2) * 100,\n\n    natural_loss_index =\n      global_natural_loss_km2 /\n      first(global_natural_loss_km2) * 100,\n\n    bfnee_share_of_loss =\n      global_bfnee_km2 /\n      global_natural_loss_km2\n  )\n\n\n\n\nGlobal BF-NEE\n\n\nShow code\nggplot(\n  global_trend,\n  aes(\n    x = year,\n    y = global_bfnee_km2\n  )\n) +\n  geom_line() +\n  geom_point() +\n  scale_y_continuous(\n    labels = comma\n  ) +\n  labs(\n    title = \"Global BF-NEE\",\n    subtitle = \"Validated countries only\",\n    x = \"Year\",\n    y = \"BF-NEE (km²)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nGlobal natural ecosystem loss\n\n\nShow code\nggplot(\n  global_trend,\n  aes(\n    x = year,\n    y = global_natural_loss_km2\n  )\n) +\n  geom_line() +\n  geom_point() +\n  scale_y_continuous(\n    labels = comma\n  ) +\n  labs(\n    title = \"Global natural ecosystem loss\",\n    subtitle = \"Validated countries only\",\n    x = \"Year\",\n    y = \"Natural ecosystem loss (km²)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nAbsolute comparison\n\n\nShow code\nglobal_trend_long &lt;- global_trend %&gt;%\n  select(\n    year,\n    global_bfnee_km2,\n    global_natural_loss_km2\n  ) %&gt;%\n  pivot_longer(\n    cols = c(\n      global_bfnee_km2,\n      global_natural_loss_km2\n    ),\n    names_to = \"indicator\",\n    values_to = \"value_km2\"\n  ) %&gt;%\n  mutate(\n    indicator = recode(\n      indicator,\n      global_bfnee_km2 =\n        \"BF-NEE\",\n      global_natural_loss_km2 =\n        \"Natural ecosystem loss\"\n    )\n  )\n\n\n\n\nShow code\nggplot(\n  global_trend_long,\n  aes(\n    x = year,\n    y = value_km2,\n    linetype = indicator\n  )\n) +\n  geom_line() +\n  geom_point() +\n  scale_y_continuous(\n    labels = comma\n  ) +\n  labs(\n    title = \"Absolute comparison\",\n    subtitle = \"Validated countries only\",\n    x = \"Year\",\n    y = \"Area (km²)\",\n    linetype = \"Indicator\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nRelative change (first period = 100)\nThis figure compares rates of change only.\nBoth indicators are normalized so that the first period equals 100.\n\n\nShow code\nglobal_trend_index_long &lt;- global_trend %&gt;%\n  select(\n    year,\n    bfnee_index,\n    natural_loss_index\n  ) %&gt;%\n  pivot_longer(\n    cols = c(\n      bfnee_index,\n      natural_loss_index\n    ),\n    names_to = \"indicator\",\n    values_to = \"index\"\n  ) %&gt;%\n  mutate(\n    indicator = recode(\n      indicator,\n      bfnee_index =\n        \"BF-NEE\",\n      natural_loss_index =\n        \"Natural ecosystem loss\"\n    )\n  )\n\n\n\n\nShow code\nggplot(\n  global_trend_index_long,\n  aes(\n    x = year,\n    y = index,\n    linetype = indicator\n  )\n) +\n  geom_line() +\n  geom_point() +\n  labs(\n    title = \"Relative change in BF-NEE and natural ecosystem loss\",\n    subtitle = \"First period = 100\",\n    x = \"Year\",\n    y = \"Index\",\n    linetype = \"Indicator\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nBF-NEE share of natural ecosystem loss\nThis figure shows the proportion of natural ecosystem loss represented by BF-NEE.\n\n\nShow code\nggplot(\n  global_trend,\n  aes(\n    x = year,\n    y = bfnee_share_of_loss\n  )\n) +\n  geom_line() +\n  geom_point() +\n  scale_y_continuous(\n    labels = percent_format(\n      accuracy = 1\n    )\n  ) +\n  labs(\n    title = \"BF-NEE as share of natural ecosystem loss\",\n    subtitle = \"Validated countries only\",\n    x = \"Year\",\n    y = \"BF-NEE / natural ecosystem loss\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nDiagnostic table\n\n\nShow code\nglobal_trend",
    "crumbs": [
      "Overview",
      "Results",
      "Global trends"
    ]
  },
  {
    "objectID": "06_regional_regressions.html",
    "href": "06_regional_regressions.html",
    "title": "Regional regressions",
    "section": "",
    "text": "Show code\nlibrary(dplyr)\nlibrary(readr)\nlibrary(ggplot2)\nlibrary(broom)\nlibrary(DT)\nlibrary(scales)\n\ndf &lt;- read_csv(\n  \"data/bfnee_regression_dataset.csv\",\n  show_col_types = FALSE\n)\n\ndf_model &lt;- df %&gt;%\n  filter(\n    !is.na(continent),\n    !is.na(log_bfnee),\n    !is.na(log_natural_loss),\n    !is.na(log_weight_sum),\n    natural_loss_sum_km2 &gt; 0,\n    bfnee_sum_km2 &gt; 0\n  )\n\n\n\nData coverage\n\n\nShow code\ndf_model %&gt;%\n  distinct(continent, iso3) %&gt;%\n  count(continent, name = \"n_countries\") %&gt;%\n  arrange(continent)\n\n\n\n  \n\n\n\n\n\nShow code\ndf_model %&gt;%\n  count(continent, year) %&gt;%\n  arrange(continent, year)\n\n\n\n  \n\n\n\n\n\nModel specification\nThe first regional consistency model is:\nlog(BF-NEE + 1) ~ log(natural ecosystem loss + 1) + log(weight sum + 1)\nwhere weight_sum represents the aggregated commodity-consumption and production-attribution component.\n\n\nBF-NEE and natural ecosystem loss\n\n\nShow code\nggplot(\n  df_model,\n  aes(\n    x = natural_loss_sum_km2,\n    y = bfnee_sum_km2\n  )\n) +\n  geom_point(alpha = 0.6) +\n  geom_smooth(method = \"lm\", se = FALSE) +\n  facet_wrap(~ continent, scales = \"free\") +\n  scale_x_continuous(labels = comma) +\n  scale_y_continuous(labels = comma) +\n  labs(\n    title = \"BF-NEE vs natural ecosystem loss\",\n    subtitle = \"Validated countries only\",\n    x = \"Natural ecosystem loss (km²)\",\n    y = \"BF-NEE (km²)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nLog-log relationship\n\n\nShow code\nggplot(\n  df_model,\n  aes(\n    x = log_natural_loss,\n    y = log_bfnee\n  )\n) +\n  geom_point(alpha = 0.6) +\n  geom_smooth(method = \"lm\", se = FALSE) +\n  facet_wrap(~ continent, scales = \"free\") +\n  labs(\n    title = \"Log-log relationship between BF-NEE and natural ecosystem loss\",\n    subtitle = \"Validated countries only\",\n    x = \"log(natural ecosystem loss + 1)\",\n    y = \"log(BF-NEE + 1)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nRegional regression models\n\n\nShow code\nfit_region &lt;- function(x) {\n  lm(\n    log_bfnee ~ log_natural_loss + log_weight_sum,\n    data = x\n  )\n}\n\nregional_models &lt;- df_model %&gt;%\n  group_by(continent) %&gt;%\n  group_modify(~ {\n    model &lt;- fit_region(.x)\n    \n    tidy_model &lt;- tidy(model)\n    glance_model &lt;- glance(model)\n    \n    tidy_model %&gt;%\n      mutate(\n        r_squared = glance_model$r.squared,\n        adj_r_squared = glance_model$adj.r.squared,\n        n_obs = glance_model$nobs\n      )\n  }) %&gt;%\n  ungroup()\n\nregional_models\n\n\n\n  \n\n\n\n\n\nShow code\ndatatable(\n  regional_models %&gt;%\n    arrange(continent, term),\n  options = list(pageLength = 25)\n)\n\n\n\n\n\n\n\n\nCoefficient comparison\n\n\nShow code\nregional_coefficients &lt;- regional_models %&gt;%\n  filter(term != \"(Intercept)\")\n\nggplot(\n  regional_coefficients,\n  aes(\n    x = continent,\n    y = estimate\n  )\n) +\n  geom_point(size = 2) +\n  geom_errorbar(\n    aes(\n      ymin = estimate - 1.96 * std.error,\n      ymax = estimate + 1.96 * std.error\n    ),\n    width = 0.15\n  ) +\n  facet_wrap(~ term, scales = \"free_y\") +\n  labs(\n    title = \"Regional regression coefficients\",\n    subtitle = \"Error bars show approximate 95% confidence intervals\",\n    x = \"Region\",\n    y = \"Coefficient estimate\"\n  ) +\n  theme_minimal() +\n  theme(\n    axis.text.x = element_text(\n      angle = 45,\n      hjust = 1\n    )\n  )\n\n\n\n\n\n\n\n\n\n\n\nModel fit by region\n\n\nShow code\nmodel_fit &lt;- regional_models %&gt;%\n  distinct(\n    continent,\n    r_squared,\n    adj_r_squared,\n    n_obs\n  )\n\nggplot(\n  model_fit,\n  aes(\n    x = reorder(continent, r_squared),\n    y = r_squared\n  )\n) +\n  geom_col() +\n  coord_flip() +\n  labs(\n    title = \"Model fit by region\",\n    x = \"Region\",\n    y = \"R²\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nResidual diagnostics\n\n\nShow code\nregional_augmented &lt;- df_model %&gt;%\n  group_by(continent) %&gt;%\n  group_modify(~ {\n    model &lt;- fit_region(.x)\n    augment(model, data = .x)\n  }) %&gt;%\n  ungroup()\n\n\n\n\nShow code\nggplot(\n  regional_augmented,\n  aes(\n    x = .fitted,\n    y = .resid\n  )\n) +\n  geom_point(alpha = 0.6) +\n  geom_hline(yintercept = 0, linetype = \"dashed\") +\n  facet_wrap(~ continent, scales = \"free\") +\n  labs(\n    title = \"Residual diagnostics by region\",\n    x = \"Fitted log(BF-NEE + 1)\",\n    y = \"Residual\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nLargest positive residuals\nCountries with large positive residuals have higher BF-NEE than expected from natural ecosystem loss and the aggregate attribution weight.\n\n\nShow code\nregional_augmented %&gt;%\n  arrange(desc(.resid)) %&gt;%\n  select(\n    continent,\n    iso3,\n    country_name,\n    transition,\n    year,\n    bfnee_sum_km2,\n    natural_loss_sum_km2,\n    weight_sum,\n    .fitted,\n    .resid\n  ) %&gt;%\n  head(30) %&gt;%\n  datatable(\n    options = list(pageLength = 15)\n  )\n\n\n\n\n\n\n\n\nLargest negative residuals\nCountries with large negative residuals have lower BF-NEE than expected from natural ecosystem loss and the aggregate attribution weight.\n\n\nShow code\nregional_augmented %&gt;%\n  arrange(.resid) %&gt;%\n  select(\n    continent,\n    iso3,\n    country_name,\n    transition,\n    year,\n    bfnee_sum_km2,\n    natural_loss_sum_km2,\n    weight_sum,\n    .fitted,\n    .resid\n  ) %&gt;%\n  head(30) %&gt;%\n  datatable(\n    options = list(pageLength = 15)\n  )",
    "crumbs": [
      "Overview",
      "Results",
      "Regional regressions"
    ]
  },
  {
    "objectID": "04_commodity_drivers.html",
    "href": "04_commodity_drivers.html",
    "title": "Commodity drivers",
    "section": "",
    "text": "Show code\nlibrary(dplyr)\nlibrary(readr)\nlibrary(tidyr)\nlibrary(ggplot2)\nlibrary(scales)\nlibrary(DT)\n\ndir_diag &lt;- \"~/Symobio/Data/Processed/BF_NEE_Diagnostics\"\ndir_bf &lt;- \"~/Symobio/Data/Processed/BF_NEE_Countries\"\n\nvalid &lt;- read_csv(\n  file.path(dir_diag, \"bfnee_valid_countries.csv\"),\n  show_col_types = FALSE\n)\n\nvalid_iso3 &lt;- valid %&gt;%\n  filter(valid_for_analysis) %&gt;%\n  pull(iso3)\n\n\n\nLoad commodity-level BF-NEE\n\n\nShow code\nread_commodity_bf &lt;- function(iso3) {\n  \n  iso3_lower &lt;- tolower(iso3)\n  dir_country &lt;- file.path(dir_bf, iso3)\n  \n  files &lt;- list.files(\n    dir_country,\n    pattern = paste0(\"^BF_NEE_by_commodity_\", iso3_lower, \"_.*\\\\.csv$\"),\n    full.names = TRUE\n  )\n  \n  if (length(files) == 0) {\n    return(NULL)\n  }\n  \n  bind_rows(lapply(files, function(f) {\n    x &lt;- read_csv(f, show_col_types = FALSE)\n    x$source_file &lt;- basename(f)\n    x\n  }))\n}\n\ncommodity_raw &lt;- bind_rows(\n  lapply(valid_iso3, read_commodity_bf)\n)\n\ncommodity_bf &lt;- commodity_raw %&gt;%\n  filter(!is.na(BF_NEE_commodity))\n\n\n\n\nGlobal commodity contributions\n\n\nShow code\ncommodity_global &lt;- commodity_bf %&gt;%\n  group_by(year, transition, commodity) %&gt;%\n  summarise(\n    bfnee_km2 = sum(BF_NEE_commodity, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\ncommodity_global_total &lt;- commodity_global %&gt;%\n  group_by(year, transition) %&gt;%\n  summarise(\n    global_bfnee_km2 = sum(bfnee_km2, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\ncommodity_global &lt;- commodity_global %&gt;%\n  left_join(\n    commodity_global_total,\n    by = c(\"year\", \"transition\")\n  ) %&gt;%\n  mutate(\n    share_global_bfnee = bfnee_km2 / global_bfnee_km2\n  )\n\n\n\n\nTop commodities by period\n\n\nShow code\ntop_commodities_by_period &lt;- commodity_global %&gt;%\n  group_by(year, transition) %&gt;%\n  slice_max(bfnee_km2, n = 15, with_ties = FALSE) %&gt;%\n  ungroup()\n\nggplot(\n  top_commodities_by_period,\n  aes(\n    x = reorder(commodity, bfnee_km2),\n    y = bfnee_km2\n  )\n) +\n  geom_col() +\n  coord_flip() +\n  facet_wrap(~ transition, scales = \"free_y\") +\n  scale_y_continuous(labels = comma) +\n  labs(\n    title = \"Top commodities by BF-NEE and transition\",\n    subtitle = \"Validated countries only\",\n    x = \"Commodity\",\n    y = \"BF-NEE (km²)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nTop commodities in latest period\n\n\nShow code\nlatest_year &lt;- max(commodity_global$year, na.rm = TRUE)\n\ntop_latest &lt;- commodity_global %&gt;%\n  filter(year == latest_year) %&gt;%\n  arrange(desc(bfnee_km2)) %&gt;%\n  slice_head(n = 20)\n\nggplot(\n  top_latest,\n  aes(\n    x = reorder(commodity, bfnee_km2),\n    y = bfnee_km2\n  )\n) +\n  geom_col() +\n  coord_flip() +\n  scale_y_continuous(labels = comma) +\n  labs(\n    title = paste(\"Top commodities in\", latest_year),\n    subtitle = \"Validated countries only\",\n    x = \"Commodity\",\n    y = \"BF-NEE (km²)\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nCommodity shares of global BF-NEE\n\n\nShow code\nggplot(\n  top_latest,\n  aes(\n    x = reorder(commodity, share_global_bfnee),\n    y = share_global_bfnee\n  )\n) +\n  geom_col() +\n  coord_flip() +\n  scale_y_continuous(labels = percent_format(accuracy = 1)) +\n  labs(\n    title = paste(\"Commodity share of global BF-NEE in\", latest_year),\n    subtitle = \"Top 20 commodities, validated countries only\",\n    x = \"Commodity\",\n    y = \"Share of global BF-NEE\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nCommodity trajectories\n\n\nShow code\ntop_overall &lt;- commodity_global %&gt;%\n  group_by(commodity) %&gt;%\n  summarise(\n    total_bfnee_km2 = sum(bfnee_km2, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  slice_max(total_bfnee_km2, n = 10, with_ties = FALSE) %&gt;%\n  pull(commodity)\n\nggplot(\n  commodity_global %&gt;% filter(commodity %in% top_overall),\n  aes(\n    x = year,\n    y = bfnee_km2,\n    group = commodity,\n    linetype = commodity\n  )\n) +\n  geom_line() +\n  geom_point() +\n  scale_y_continuous(labels = comma) +\n  labs(\n    title = \"BF-NEE trajectories for top commodities\",\n    subtitle = \"Top 10 commodities by total BF-NEE across all periods\",\n    x = \"Year\",\n    y = \"BF-NEE (km²)\",\n    linetype = \"Commodity\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nDiagnostic table\n\n\nShow code\ndatatable(\n  commodity_global %&gt;%\n    arrange(desc(bfnee_km2)),\n  options = list(pageLength = 25)\n)",
    "crumbs": [
      "Overview",
      "Results",
      "Commodity drivers"
    ]
  },
  {
    "objectID": "01_processing_status.html",
    "href": "01_processing_status.html",
    "title": "Processing status and scale diagnostics",
    "section": "",
    "text": "Show code\nlibrary(dplyr)\nlibrary(readr)\nlibrary(ggplot2)\nlibrary(DT)\n\ndir_diag &lt;- \"~/Symobio/Data/Processed/BF_NEE_Diagnostics\"\n\ndiag &lt;- read_csv(\n  file.path(dir_diag, \"bfnee_country_diagnostics_all_transitions.csv\"),\n  show_col_types = FALSE\n)\n\nvalid &lt;- read_csv(\n  file.path(dir_diag, \"bfnee_valid_countries.csv\"),\n  show_col_types = FALSE\n)\n\n\n\nSummary\n\n\nShow code\nvalid %&gt;%\n  count(valid_for_analysis)\n\n\n\n  \n\n\n\n\n\nShow code\nggplot(valid, aes(x = valid_for_analysis)) +\n  geom_bar() +\n  labs(\n    title = \"Countries passing BF-NEE diagnostics\",\n    x = \"Valid for analysis\",\n    y = \"Number of countries\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nOld-scale outputs by transition\n\n\nShow code\nscale_summary &lt;- diag %&gt;%\n  count(transition, suspicious_old_scale)\n\nscale_summary\n\n\n\n  \n\n\n\n\n\nShow code\nggplot(scale_summary, aes(x = transition, y = n, fill = suspicious_old_scale)) +\n  geom_col() +\n  labs(\n    title = \"Suspicious old-scale outputs by transition\",\n    x = \"Transition\",\n    y = \"Number of country-transition outputs\",\n    fill = \"Old scale?\"\n  ) +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\n\n\nCountries excluded from analysis\n\n\nShow code\nexcluded &lt;- valid %&gt;%\n  filter(!valid_for_analysis)\n\ndatatable(excluded)\n\n\n\n\n\n\n\n\nDiagnostic table\n\n\nShow code\ndatatable(\n  diag %&gt;%\n    arrange(desc(suspicious_old_scale), iso3, transition),\n  options = list(pageLength = 25)\n)",
    "crumbs": [
      "Overview",
      "Project",
      "Processing status"
    ]
  },
  {
    "objectID": "05_maps.html",
    "href": "05_maps.html",
    "title": "",
    "section": "",
    "text": "ResultsInteractive maps Code",
    "crumbs": [
      "Overview",
      "Results",
      "Interactive maps"
    ]
  }
]